搭建一个简单的脚手架
安装
# install it globally
$ npm install -g lxh-wind-cli

# or yarn
$ yarn global add lxh-wind-cli
使用
Quick Start
$ wind create <name> [-f|--force]
Options
-f, --force: Overwrite if the target exists